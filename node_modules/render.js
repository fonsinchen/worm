
exports = function(bound, driver) {
    return {
        update : function(objs) {},
        insert : function(objs) {},
        "delete" : function(params) {}, // semantic is unclear: only delete from master table or from all?
        select : function(params) {}
    }
}