exports = function(expression) {
    // TODO: parse, create syntax tree
    // TODO: create expression without redefining the functions every time
    return {
        render : function(driver) {},
        eval : function(params) {}
    }
}